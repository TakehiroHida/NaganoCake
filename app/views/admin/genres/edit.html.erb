<h1>ジャンル編集</h1>

<%= form_with model: @genre, url: admin_genre_path do |f| %>
    <div>
        <h3>ジャンル名</h3>
            <%= f.text_field :name %>
    </div>
    
    <div>
        <%= f.submit '変更を保存' %>
    </div>
<% end %>